---
description: >
  Redirect shim — this skill has moved to /mvts/setup.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has moved. Use `/mvts/setup` instead.

Immediately say:
> "`/mvts-setup` has moved to `/mvts/setup`. Continuing there now."

Then proceed as if `/mvts/setup` was invoked with the same arguments.
