---
description: >
  Redirect shim — this skill has moved to /mvts/dev/influxdb.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has moved. Use `/mvts/dev/influxdb` instead.

Immediately say:
> "`/mvts-influxdb` has moved to `/mvts/dev/influxdb`. Continuing there now."

Then proceed as if `/mvts/dev/influxdb` was invoked with the same arguments.
