---
description: >
  Redirect shim — this skill has moved to /mvts/dev/idc-validate.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has moved. Use `/mvts/dev/idc-validate` instead.

Immediately say:
> "`/mvts-idc-validate` has moved to `/mvts/dev/idc-validate`. Continuing there now."

Then proceed as if `/mvts/dev/idc-validate` was invoked with the same arguments.
