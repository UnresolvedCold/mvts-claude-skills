---
description: >
  Redirect shim — this skill has moved to /mvts/ops/relay.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has moved. Use `/mvts/ops/relay` instead.

Immediately say:
> "`/mvts-relay-debug` has moved to `/mvts/ops/relay`. Continuing there now."

Then proceed as if `/mvts/ops/relay` was invoked with the same arguments.
