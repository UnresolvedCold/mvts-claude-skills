---
description: >
  Redirect shim — this skill has moved to /mvts/ops/vtm-stuck.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has moved. Use `/mvts/ops/vtm-stuck` instead.

Immediately say:
> "`/mvts-vtm-aisle-stall` has moved to `/mvts/ops/vtm-stuck`. Continuing there now."

Then proceed as if `/mvts/ops/vtm-stuck` was invoked with the same arguments.
