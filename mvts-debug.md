---
description: >
  Redirect shim — this skill has moved to /mvts/index.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has been reorganized. Use `/mvts/index` instead.

Immediately tell the user:
> "The `/mvts-debug` skill has moved to `/mvts/index`. I'll continue with `/mvts/index` now."

Then proceed as if `/mvts/index` was invoked with the same arguments.
