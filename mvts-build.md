---
description: >
  Redirect shim — this skill has moved to /mvts/dev/build.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has moved. Use `/mvts/dev/build` instead.

Immediately say:
> "`/mvts-build` has moved to `/mvts/dev/build`. Continuing there now."

Then proceed as if `/mvts/dev/build` was invoked with the same arguments.
