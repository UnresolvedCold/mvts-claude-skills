---
description: >
  Redirect shim — this skill has moved to /mvts/ops/config.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has moved. Use `/mvts/ops/config` instead.

Immediately say:
> "`/mvts-config` has moved to `/mvts/ops/config`. Continuing there now."

Then proceed as if `/mvts/ops/config` was invoked with the same arguments.
