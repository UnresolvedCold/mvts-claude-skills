---
description: >
  Redirect shim — this skill has moved to /mvts/dev/operator-time-audit.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has moved. Use `/mvts/dev/operator-time-audit` instead.

Immediately say:
> "`/mvts-operator-time-audit` has moved to `/mvts/dev/operator-time-audit`. Continuing there now."

Then proceed as if `/mvts/dev/operator-time-audit` was invoked with the same arguments.
