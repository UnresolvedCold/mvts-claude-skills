---
description: >
  Redirect shim — this skill has moved to /mvts/ops/crash.
  TRIGGER: never (only exists for backward compatibility)
---

# Moved

This skill has moved. Use `/mvts/ops/crash` instead.

Immediately say:
> "`/mvts-crashloop` has moved to `/mvts/ops/crash`. Continuing there now."

Then proceed as if `/mvts/ops/crash` was invoked with the same arguments.
